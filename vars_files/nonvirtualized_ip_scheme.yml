#fabric_ip_scheme

# Need to update the UL subnets as they are currently the same for all distros
subnet: 10.4.0.0/16

core_subnet: "{{ subnet | ipsubnet(20, 0) }}" # "10.4.0.0/20"
internet_edge_inside_subnet: "{{ subnet | ipsubnet(20, 1) }}" # "10.4.16.0/20"
wan_agg_subnet: "{{ subnet | ipsubnet(20, 2) }}" # "10.4.32.0/20"
datacenter_subnet: "{{ subnet | ipsubnet(20, 3) }}" # "10.4.48.0/20"
distro_3_subnet: "{{ subnet | ipsubnet(20, 4) }}" # "10.4.64.0/20"
distro_4_subnet: "{{ subnet | ipsubnet(20, 5) }}" # "10.4.80.0/20"
distro_3_subnet: "{{ subnet | ipsubnet(20, 6) }}" # "10.4.96.0/20"
distro_4_subnet: "{{ subnet | ipsubnet(20, 7) }}" # "10.4.112.0/20"
distro_5_subnet: "{{ subnet | ipsubnet(20, 8) }}" # "10.4.128.0/20"
distro_6_subnet: "{{ subnet | ipsubnet(20, 9) }}" # "10.4.144.0/20"
shared_services_subnet: "{{ subnet | ipsubnet(20, 10) }}" # "10.4.160.0/20"
remote_datacenter_subnet: "{{ subnet | ipsubnet(20, 11) }}" # "10.4.176.0/20"

# Derived Subnet Variables
Core_aggregate: "{{ Core_subnet | ipsubnet }}"

D1_aggregate: "{{ distro_1_subnet | ipsubnet }}"
D1_Loop_subnet: "{{ distro_1_subnet | ipsubnet(24, 0) }}"
D1DL11: "{{ D1_aggregate | ipsubnet(30, 64) }}"
D1DL12: "{{ D1_aggregate | ipsubnet(30, 65) }}"
D1DL21: "{{ D1_aggregate | ipsubnet(30, 66) }}"
D1DL22: "{{ D1_aggregate | ipsubnet(30, 67) }}" 
D1UL11: "{{ Core_aggregate | ipsubnet(30, 75) }}"
D1UL12: "{{ Core_aggregate | ipsubnet(30, 76) }}"
D1UL21: "{{ Core_aggregate | ipsubnet(30, 139) }}"
D1UL22: "{{ Core_aggregate | ipsubnet(30, 140) }}"

D2_aggregate: "{{ distro_2_subnet | ipsubnet }}"
D2_Loop_subnet: "{{ distro_2_subnet | ipsubnet(24, 0) }}"
D2DL11: "{{ D2_aggregate | ipsubnet(30, 64) }}"
D2DL12: "{{ D2_aggregate | ipsubnet(30, 65) }}"
D2DL21: "{{ D2_aggregate | ipsubnet(30, 66) }}"
D2DL22: "{{ D2_aggregate | ipsubnet(30, 67) }}" 
D2UL11: "{{ Core_aggregate | ipsubnet(30, 75) }}"
D2UL12: "{{ Core_aggregate | ipsubnet(30, 76) }}"
D2UL21: "{{ Core_aggregate | ipsubnet(30, 139) }}"
D2UL22: "{{ Core_aggregate | ipsubnet(30, 140) }}"


D3_aggregate: "{{ D3_subnet | ipsubnet }}"
D3_Loop_subnet: "{{ D3_aggregate | ipsubnet(24, 0) }}"
D3DL11: "{{ D3_aggregate | ipsubnet(30, 64) }}"
D3DL12: "{{ D3_aggregate | ipsubnet(30, 65) }}"
D3DL21: "{{ D3_aggregate | ipsubnet(30, 66) }}"
D3DL22: "{{ D3_aggregate | ipsubnet(30, 67) }}" 
D3DL31: "{{ D3_aggregate | ipsubnet(30, 72) }}"
D3DL32: "{{ D3_aggregate | ipsubnet(30, 73) }}" 
D3UL11: "{{ Core_aggregate | ipsubnet(30, 75) }}"
D3UL12: "{{ Core_aggregate | ipsubnet(30, 76) }}"
D3UL21: "{{ Core_aggregate | ipsubnet(30, 139) }}"
D3UL22: "{{ Core_aggregate | ipsubnet(30, 140) }}"

D4_aggregate: "{{ distro_4_subnet | ipsubnet }}"
D4_Loop_subnet: "{{ distro_4_subnet | ipsubnet(24, 0) }}"
D4DL11: "{{ D4_aggregate | ipsubnet(30, 64) }}"
D4DL12: "{{ D4_aggregate | ipsubnet(30, 65) }}"
D4DL21: "{{ D4_aggregate | ipsubnet(30, 66) }}"
D4DL22: "{{ D4_aggregate | ipsubnet(30, 67) }}" 
D4UL11: "{{ Core_aggregate | ipsubnet(30, 75) }}"
D4UL12: "{{ Core_aggregate | ipsubnet(30, 76) }}"
D4UL21: "{{ Core_aggregate | ipsubnet(30, 139) }}"
D4UL22: "{{ Core_aggregate | ipsubnet(30, 140) }}"

D5_aggregate: "{{ distro_5_subnet | ipsubnet }}"
D5_Loop_subnet: "{{ distro_5_subnet | ipsubnet(24, 0) }}"
D5DL11: "{{ D5_aggregate | ipsubnet(30, 64) }}"
D5DL12: "{{ D5_aggregate | ipsubnet(30, 65) }}"
D5DL21: "{{ D5_aggregate | ipsubnet(30, 66) }}"
D5DL22: "{{ D5_aggregate | ipsubnet(30, 67) }}" 
D5UL11: "{{ Core_aggregate | ipsubnet(30, 75) }}"
D5UL12: "{{ Core_aggregate | ipsubnet(30, 76) }}"
D5UL21: "{{ Core_aggregate | ipsubnet(30, 139) }}"
D5UL22: "{{ Core_aggregate | ipsubnet(30, 140) }}"

D6_aggregate: "{{ distro_6_subnet | ipsubnet }}"
D6_Loop_subnet: "{{ distro_6_subnet | ipsubnet(24, 0) }}"
D6DL11: "{{ D6_aggregate | ipsubnet(30, 64) }}"
D6DL12: "{{ D6_aggregate | ipsubnet(30, 65) }}"
D6DL21: "{{ D6_aggregate | ipsubnet(30, 66) }}"
D6DL22: "{{ D6_aggregate | ipsubnet(30, 67) }}" 
D6UL11: "{{ Core_aggregate | ipsubnet(30, 75) }}"
D6UL12: "{{ Core_aggregate | ipsubnet(30, 76) }}"
D6UL21: "{{ Core_aggregate | ipsubnet(30, 139) }}"
D6UL22: "{{ Core_aggregate | ipsubnet(30, 140) }}"


